{
  "type": "module",
  "dependencies": {
    "firebase": "^12.12.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "happy-dom": "^20.9.0",
    "vitest": "^1.6.1"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  }
}
